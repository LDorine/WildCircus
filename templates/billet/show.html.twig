{% extends 'base.html.twig' %}

{% block title %}Billet{% endblock %}

{% block body %}
    <h1>Billet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ billet.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ billet.nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ billet.email }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ billet.date ? billet.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ billet.ville }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('billet_index') }}">back to list</a>

    <a href="{{ path('billet_edit', {'id': billet.id}) }}">edit</a>

    {{ include('billet/_delete_form.html.twig') }}
{% endblock %}
